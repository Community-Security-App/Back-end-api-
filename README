#Install 

Make sure you have express, node.js, mongodb and npm(manages packages for node)

git clone the repo 

CD into it 

run npm install to get all the dependencie

Run node server.js to run the application. Make sure an instance of mongodb is running to link to
